FAILURE INJECTION — W0b, ядро диспетчера. N=1.
Справжнє: git, файлова система, процеси, SIGKILL, лізи, реконсиляція.
Симульоване: відповідь GitHub API (підставний gh у PATH) — і сценарій А
прогнано ДВІЧІ, другий раз узагалі без gh, на чистому git.

══════════════════════════════════════════════════════════════════════════════
✅ PASS  А1. Убито ПІСЛЯ merge, ДО запису стану (PR змерджено, gh відповідає MERGED)
──────────────────────────────────────────────────────────────────────────────
  убито в точці      : after-merge-before-state
  на диску після kill: state = merging
  що каже світ       : робота в main: ТАК, merge-комітів: 1, gh: MERGED
  вердикт реконсиляції: done (attempts 0)
  очікувалось        : done
  дубль роботи       : ні
  втрата роботи      : ні
  PR #51 MERGED (4de94639e696496a40a1d70d45a3b837b2dfb825) — робота вже у проді, attempts не росте

══════════════════════════════════════════════════════════════════════════════
✅ PASS  А2. Те саме БЕЗ симуляції: без PR, звірка чистим git (§4.2 рядок 2)
──────────────────────────────────────────────────────────────────────────────
  убито в точці      : after-merge-before-state
  на диску після kill: state = merging
  що каже світ       : робота в main: ТАК, merge-комітів: 1, gh НЕ використовується
  вердикт реконсиляції: done (attempts 0)
  очікувалось        : done
  дубль роботи       : ні
  втрата роботи      : ні
  гілка cycle/m2-fi вже в main — робота у проді, attempts не росте

══════════════════════════════════════════════════════════════════════════════
✅ PASS  Б. Убито ПІД ЧАС гейта (робота не змерджена)
──────────────────────────────────────────────────────────────────────────────
  убито в точці      : during-gate
  на диску після kill: state = gated
  що каже світ       : робота в main: ні, гілка існує, PR немає
  вердикт реконсиляції: ready (attempts 1)
  очікувалось        : ready (attempts += 1)
  дубль роботи       : ні
  втрата роботи      : ні
  гілка cycle/m2-fi є, але PR не відкритий — робота почалась і не дійшла до PR · ліза лишилась epoch=1 (наступний клейм дасть 2 — старий воркер відсічений CAS)

══════════════════════════════════════════════════════════════════════════════
✅ PASS  В. strw-state незаписуваний — тік НЕ починається
──────────────────────────────────────────────────────────────────────────────
  убито в точці      : не застосовне: тік не стартував
  на диску після kill: state = item-файли не чіпались
  що каже світ       : strw-state chmod 0500, state.md незмінений: ТАК
  вердикт реконсиляції: тік НЕ почався (attempts 0)
  очікувалось        : тік НЕ почався
  дубль роботи       : ні
  втрата роботи      : ні
  strw-state незаписуваний (/var/folders/vz/3bgrbdq90vj50nj5sty0_py00000gp/T/fi-c-state-56e08b83aa66335/strw-state) — тік НЕ починається (§3.1). 27.07 цей стан тривав 5 год 52 хв, поки продуктове репо робило 10 комітів.

══════════════════════════════════════════════════════════════════════════════
ПІДСУМОК: 3/3 сценаріїв
  А (після merge, до запису): PASS
  Б (під час гейта)         : PASS
  В (незаписуваний state)   : PASS
  дубль роботи: немає · втрата роботи: немає

3/3. KILL-критерій §13 НЕ спрацював.
══════════════════════════════════════════════════════════════════════════════
