# AGENTS.md — правила для агентів у цьому репозиторії
<!-- Шаблон strw-factory. Заповни <...> при скаффолді (strw-product-init). -->
<!-- Ритуал: агент помилився → додай сюди ОДНЕ правило, що запобігає повтору. -->

## Продукт
<product-id> · <одне речення що це> · PRD: strw-state/products/<product-id>/prd.md

## Стек
<з company-context: мова, фреймворк, БД, хостинг>

## Конвенції
- Мова коду/комітів: EN; коміти: conventional commits.
- Структура: <src/, tests/, tracking/>.
- Стиль: <linter+formatter, конфіг у репо — не обговорюється>.

## Hard rules
1. Tests-first: AC з PRD → падаючі тести → реалізація. Тести під готовий код не рахуються.
2. Новий пакет — тільки після верифікації в реєстрі (існує, живий); dep-audit чистий.
3. Секрети — тільки env; жодного ключа в коді чи конфігу.
4. Tracking events за tracking-plan.md — разом із фічею, не «потім».
5. Scope = PRD після G2. Фіча поза scope → спершу питання в triage-inbox.
6. Прод-деплой/публічний реліз — заборонено з цього репо агентом; тільки gate G3.

## Workflow
- Стан циклу: strw-state/products/<product-id>/state.md (Done/Next/Tried & failed) — читати перед роботою, писати після.
- CI (ci.yml) зелений = єдиний прийнятний доказ «працює».

## Правила, додані після помилок
<!-- append-only: дата · що сталось → правило -->
