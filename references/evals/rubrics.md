# Eval Rubrics — планка це eval, а не демо

Checker оцінює артефакт за рубрикою, не «на око». Використання: (а) checker phase кожної петлі; (б) регресія після зміни промпту/скіла/паспорта (self-improvement.md); (в) калібрування нового checker'а проти golden-набору (`evals/golden/`).

## Два рівні перевірки (порядок фіксований)

0. **Структурний (детермінований):** `scripts/validate-artifact.sh <type> <file>` — обов'язкові секції контракту. FAIL → артефакт повертається maker'у БЕЗ виклику LLM-checker'а. Структурні фейли ніколи не йдуть в inbox.
1. **Змістовий (LLM-rubric):** нижче. Вердикт PASS лише коли всі виміри «прохідні».

## Спільні виміри (усі критичні артефакти)

| Вимір | Прохідний опис |
|-------|----------------|
| Task success | Артефакт розв'язує задачу стадії (не «схоже на звіт», а відповідає на питання gate) |
| Data integrity | Повний чекліст data-integrity-protocol.md: джерело/період/довіра для кожної цифри |
| **Trajectory compliance** | Trace maker'а відповідає паспорту: читав state, виконав обов'язкові перевірки, не виходив за scope, не пропускав кроки. Гарний вихід із пропущеними перевірками = FAIL |
| Аргументація | Кожне ключове твердження: твердження → доказ → джерело; контраргументи не замовчані |

## Per-artifact доповнення

- **idea-card:** сигнали попиту незалежні (≥2); для треку C замість сигналів — проба спростування, що вміє почервоніти за ≤ тиждень (L1-discovery 2a); ICE обґрунтований, не дубль Kill log.
- **validation-report:** TAM bottom-up перевірений, «конкурентів нема» розкрито, найдешевша перевірка реально найдешевша, рекомендація випливає з даних (не з тону).
- **prd:** метрики успіху вимірювані (число+період), НЕ-цілі змістовні, AC тестопридатні (стануть тестами ДО коду), tracking events покривають метрики успіху.
- **build-report:** diff vs PRD чесний (недороблене назване), тести написані до/разом з кодом і покривають AC+edge cases, security-секція заповнена (dep-audit, верифікація залежностей), CI-лінк живий.
- **persona-card:** evidence-теги чесні ([E] = живий лінк + період + довіра; [E] без джерела = FAIL); немає каррикатури/стереотипного дрейфу — unexpected_trait справді контрстереотипна, атрибути не «дописані» моделлю в позитив (дописане = [A]); анти-персона змістовна (реальний сегмент, який спокушає скоуп, а не опудало); tracking_binding реалістичний проти tracking-plan продукту (events/props існують або заплановані).
- **copy-guide:** кожне правило в `rules` має пояснення в прозі і навпаки (розбіжність = FAIL); словник забороняє синоніми, які СПРАВДІ трапляються (перевірити прогоном лінтера на живому коді, не повірити списку); «Що виміряно» — числа з прогону, не переказ; заборони не суперечать чинній design-spec, а якщо суперечать — конфлікт названий у «Не встановлено» з адресатом-рішенням, не замовчаний.
- **design-research:** «інші» — справжні продукти з конкретною механікою (назва без механіки = FAIL); «що відкидаємо» має причину, здатну зупинити майбутнє «покращення»; висновок випливає з розбору, а не дописаний під готовий макет (дата research ≤ дата макета).
- **design-options:** варіанти справжні — кожен міг би виграти в іншого продукту (опудало = FAIL); критерії названі до порівняння, не підігнані під переможця; у програшного названа умова повернення.
- **growth-report:** цифри лише з metrics.md/PRD, чернетки без вигаданих тверджень.
- **portfolio-brief:** метрики фабрики повні (включно з first-pass accept rate і вартістю петель з loops-log), фокуси actionable, тренди чесні (2 точки ≠ тренд).
- **regression-report:** «зелено» без коду виходу власного `check` результатом не є; повнота гейтів звіряється ЧИСЛОМ (`ran + skipped == declared`, `errors ⊆ ran`); rc=3 хешера — зупинка, не «без змін»; кожна червона лишила finding або id елемента.
- **checker-verdict:** проходи справді зроблені, а не перелічені; детермінована дія має команду І вивід; кожна знахідка несе сценарій відмови; `Important`, поданий як відкладений, має id елемента (форму примушує рівень 0 — `review-policy.md`).

## Регресія (після будь-якої зміни промпту/скіла/паспорта)

**Виконавець — `scripts/evals/run.sh`, не обіцянка.** Сіді-кейси більше не живуть лише прозою в кінці golden'а (там вони лишились як пояснення, чому фікстура існує): кожен — файл `references/evals/seeded/<type>/<case>.md` з полями `type`, `expect`, `level`, `defect`, `defect_keywords`, `source`.

- `run.sh --offline` (типово, і саме він у pre-commit та `release.sh`): golden мусить дати PASS рівня 0, seeded із `level: 0` — FAIL рівня 0.
- `STRW_EVALS_ONLINE=1 run.sh`: seeded із `level: 1` ідуть LLM-чекеру (модель — з `strw-state/engine/lanes.yaml` для чекера відповідної петлі). Вердикт — перший рядок відповіді; FAIL зараховується лише коли у відповіді згаданий саме цей дефект. Golden у цьому режимі мусять дати PASS — це позитивний контроль: набір, де все очікує FAIL, зеленів би для чекера, який завжди каже FAIL.
- Нуль знайдених фікстур — код 2, не успіх.

Регресія не пройшла → зміна не застосовується, версія не бампається.
