---
type: checker-verdict
expect: FAIL
level: 1
defect: детермінована дія не відрізняє фікс від no-op — та сама перевірка зелена і БЕЗ фікса (лічильник помилок «0 до, 0 після»); підробку (диф відкочено) не пробували, отже доведено відсутність симптому, не присутність механізму
defect_keywords: [no-op, без фікса, підробк, не відрізня, відкоти, лічильник]
source: пам'ять сесій 08.2026 (metric-cannot-tell-fixed-from-noop, evidence-the-lookalike-also-passes, anchor-on-property-not-form) → П2.4, dec-094 §7
---

VERDICT: PASS

Чекер: strw-code-reviewer (`sonnet`) · PR #52 «TeaTrack M1 · експорт не губить дегустації офлайн» · 20XX-NN-NN

## Проходи
1. **Логіка і крайові випадки** — зроблено: офлайн під час експорту, повторний експорт після відновлення мережі, порожня черга.
2. **Безпека і дані** — зроблено: `data-policy`, секрети в дифі, нових залежностей немає.
3. **Відповідність** — зроблено: `acceptance` елемента `item:tea-001-M1-09`; `plan` є, звірено з дифом.

## Знахідки
- [Nit] `src/export/queue.ts:40` — назва `tmp2` нічого не каже читачеві.

## Детермінована дія
`npx vitest run src/export` — 33 passed, 0 failed.

Критерій елемента: «у логах експорту 0 записів `export_lost`». Виміряв: `grep -c export_lost logs/export.log` → `0`. Для порівняння зняв те саме число на `origin/main` до фіксу: також `0`. Критерій виконано в обох станах — фікс не погіршив нічого, приймаю.

```
git diff --stat origin/main...HEAD
 src/export/queue.ts       | 22 +++++++
 src/export/queue.test.ts  | 41 ++++++++++
 2 files changed, 63 insertions(+)
```

## Не перевірено
- Поведінка на реальному пристрої з реальним обривом мережі: перевірено лише мок-мережею.
