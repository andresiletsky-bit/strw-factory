# Artifact Contracts — фіксовані схеми артефактів між стадіями

Урок MetaGPT: агенти обмінюються структурованими документами, не вільним чатом. Gate НЕ приймає артефакт із пропущеними обов'язковими полями. Шаблони рендеряться через Template Library (grow-pm), ці контракти — мінімальні обов'язкові поля.

**Структуру перевіряє скрипт, не LLM:** канонічні секції кожного типу зашиті в `scripts/validate-artifact.sh` — він запускається ПЕРЕД LLM-checker'ом (рівень 0). Еталони: `references/evals/golden/`. Зміст оцінюється за `references/evals/rubrics.md`.

**Секція «Не встановлено» (обов'язкова для validation-report, portfolio-brief, retro-note, design-delta; примус рівня 0).** Місце для невідомого: питання, на які джерела не відповіли, кожне — з назвою джерела, що мало б відповісти (і чому не відповіло, якщо відомо). Куплено ретро W33 (рішення CEO 14.08): коли в контракті немає місця для невідомого, воно витікає в аргументацію найправдоподібнішим наративом — L5 вигадала причину триденного простою з поля scratch-файлу задачі про саму себе. Порожньою секція бути не може: якщо невідомого справді нема, пишеться «Всі питання цього запуску закриті джерелами» — явним рядком, не відсутністю секції.

## idea-card.md (вихід L1 / ручний intake)
Обов'язково: Назва · Проблема (чий біль, як розв'язують зараз) · Сигнали попиту (треки A/B: ≥2 джерела з лінками; трек C: замість сигналів — проба спростування ≤ тижня, L1-discovery 2a) · Тип (SaaS/Mobile/AI/Content) · Гіпотеза монетизації · Чому ми/чому зараз · Перша оцінка ICE (I/C/E 1–10 + сума) · Гіпотеза аудиторії (1–2 прото-персони, по ≤3 рядки — хто і чому; НЕ повні persona-card).

## validation-report.md (вихід L2, вхід G1)
Обов'язково: TAM/SAM/SOM з методом розрахунку · Конкуренти (≥3, ціни, слабкості) · Попит (докази, не припущення — data-integrity-protocol) · Ризики (ринкові/технічні/легальні) · Найдешевший спосіб перевірки гіпотези · ICE фінальний · **Рекомендація BUILD/KILL/PIVOT + аргументи за/проти** · Розділ critic-review (заперечення checker'а і відповіді на них) · **Не встановлено** (див. вступ) · Персони (predicted): 3–5 карт за контрактом persona-card у `products/<id>/personas.md` — з того самого VoC-матеріалу, що й звіт (рівень 0 примусить після оновлення golden; зафіксовано при введенні контракту).

## prd.md (вихід стадії Definition, вхід G2)
Обов'язково: Проблема і цілі · Метрики успіху (число + період; сегментовані по персонах, де це вимірюване) · Scope MVP · НЕ-цілі · User stories з acceptance criteria (кожна прив'язана до persona_id з `personas.md`) · Залежності/ризики · Tracking-вимоги (events; включно з cohort_definition персон) · Оцінка обсягу.

## personas.md — persona-card (вихід L2 разом зі звітом; живе далі всі стадії)
Один файл на продукт (`products/<id>/personas.md`), 3–5 карт, кожна — YAML-блок (```yaml) + ≤5 рядків прози; одна з карт — анти-персона. Схема YAML — шаблон `_brain/_templates/persona-card.md` (концепт [[STRW_Persona_Layer_v1]] розд. 3, дослівно). Обов'язкові поля кожної карти: `persona_id` (`<product>-p<N>`) · `archetype` · `status` (predicted/calibrated/deprecated) · `weight` + `weight_source` · `demographics`/`context`/`behavior` з тегом [E]/[I]/[A] на кожному атрибуті · ≥1 [E]-джерело в `context` (лінк + період + довіра, як data-integrity) · `unexpected_trait` з [E] (анти-каррикатура) · `anti_persona` · `tracking_binding` (cohort_definition + calibration_metrics проти tracking-plan) · `open_assumptions` (усі [A] — черга на перевірку) · `changelog`. Частка [A]-атрибутів файлу ≤50% — інакше це фантазія, не персони. Synthetic ≠ evidence: карти й панельні виходи ніколи не стоять у «Попит»/«Сигнали попиту» і не входять у G-рішення (data-integrity-protocol, клас `synthetic`).

## design-handoff (стадія Design)
За design-bridge: токени, компоненти, стани, breakpoints, WCAG 2.1 AA — blocker.

## copy-guide.md (стайлгайд текстів продукту; живе всі стадії)
Обов'язково: Що виміряно (числа з прогону `copy-lint.py`, не оцінка на око) · Звертання (одне на продукт, рішенням CEO) · Тон і його межі · Словник (канон + заборонені синоніми + чому) · Шаблони (кнопка/помилка/порожній стан/незворотна дія) · Чорний список (що на екран не потрапляє ніколи) · Мови · Не встановлено (див. вступ) · **машинний блок ```yaml з ключем `rules`** — його читає `copy-lint.py`; гайд без блоку — проза без гейта, рівень 0 його не пропускає. Правило, дописане в прозі й забуте в `rules`, — побажання; правило в `rules` без пояснення в прозі — свавілля лінтера. Обидва боки тримає один файл.

## design-research (стадія Design, ДО макета; рекомендація, не гейт — рішення CEO 2026-08-24)
Обов'язково: Задача і контекст · Як це розв'язують інші (≥3 названі продукти, що саме в кожного взято оком — скріншот або конкретний опис механіки, не «в X добре зроблено») · Що беремо · Що свідомо відкидаємо (з причиною — інакше наступна сесія «покращить» назад) · Ризики вибору · Не встановлено (див. вступ).

## design-options (стадія Design, для ризикових екранів)
Обов'язково: Варіанти (≥2 СПРАВЖНІ — варіант, намальований щоб програти, не рахується) · Критерії порівняння (названі ДО порівняння) · Обраний і чому · Програшний і чому збережений (умова, за якої повернемось до нього) · Не встановлено (див. вступ).

## design-delta (вихід L7, вхід критика L7)
Обов'язково: Змінені одиниці (`ref` + `working_files` + старий/новий хеш — шляхи беруться зі звіту `design-hash.py`, не з `ref`) · Спільні причини (токени з РІЗНИЦІ сигнатур і одиниці, які вони пояснюють; токен, присутній в обох знімках, причиною бути не може) · Непояснене (одиниці без спільної причини, по групі на кожну; >5 груп — потоп, rc=4, ескалація question) · Нечитані джерела (rc=3 хешера чи емітера, `unwatched`-одиниці й давність їх звірки — «не звірялось» ніколи не подається як «без змін») · **Не встановлено** (див. вступ). Заведення елементів реєстру в цей контракт НЕ входить: жоден вузол L7 сьогодні його не робить.

## regression-report.md (вихід тижневої регресії L8, спека L7-design розд. 8)
Обов'язково: Фаза 1 · Технічні гейти (**числа `declared` / `ran` / `red` / `skipped` / `errors` зі звіту `regression-phase1.sh`** — повноту звіряє ЧИСЛО, не перелічування; далі кожен гейт: ім'я, код виходу його ж `check`, `skipped:<owner>` для не-агентських — словник має ТРИ значення (`agent` · `human` · `missing-tooling`), і закривати два останні заборонено (`strw-run.sh:418`); `error:not-runnable` для rc 126/127 — «команду не запустити» ≠ «продукт не пройшов гейт»; «зелено» без коду виходу результатом не є — прецедент `kmp`, що віддавав UP-TO-DATE і exit 0) · Фаза 2 · Синк дизайну (повний прогін L7: rc `design-hash.py`, rc емітера, `unwatched`-одиниці й давність їх звірки; rc=3 — ЗУПИНКА, не «без змін») · Фаза 3 · UI (доки оренди симулятора немає — заявлене «НЕ ВИКОНУВАЛАСЬ» із причиною; мовчазна відсутність секції заборонена, бо читалась би як «зроблено») · Що лишила кожна червона (finding у inbox або id елемента реєстру — на КОЖНУ червону фазу; червона без сліду = звіт недійсний) · **Не встановлено** (див. вступ).

## build-report.md (вихід L3, вхід G3)
Обов'язково: Що реалізовано vs PRD (diff scope) · Статус тестів (лінк на CI; тести написані з AC до коду) · **Security: dep-audit чистий + нові залежності верифіковані в реєстрі (без галюцинованих/typosquat) + security-review по diff** · Tracking plan покриття · Відомі обмеження · Чеклист deploy-checklist пройдено · Code-review вердикт checker'а.

## launch-checklist.md (стадія QA & Launch)
Обов'язково: тести пройдені · аналітика верифікована на staging · **security-блокер: dep-audit + secret scan + security-review закриті (аналог WCAG у design-handoff)** · rollback-план · сторінка продукту · ціни.

## growth-report.md (вихід L4)
Обов'язково: Кампанії за тиждень · Метрики (трафік, конверсія, CAC якщо є) · Контент опубліковано/у черзі · Наступний тиждень.

## portfolio-brief.md (вихід L5, головний документ тижня)
Обов'язково: Стан кожного продукту (стадія, ключова метрика, тренд) · Метрики фабрики (throughput, kill-rate, autonomy ratio, inbox lag, budget факт · **економіка harness з loops-log: first-pass accept rate, середні ітерації maker↔checker, вартість/тривалість запуску петлі + тренд**) · **Kill-ризики продуктів у проді (pact-001: R1/R2 за правилом рішення з паспорта L5 — частка, вибірка, вікно, статус; до першого прод-релізу — чесне «н/д до M4», без вигаданих чисел)** · 1–3 фокуси наступного тижня · Кандидати на KILL/подвійну ставку · Питання, що чекають рішення в inbox · **Не встановлено** (див. вступ — питання тижня, на які джерела не відповіли; сюди ж лягає «причина X не встановлена», замість правдоподібного наративу в аргументації).

## retro-note.md (вихід L6, briefs/)
Обов'язково: Що спрацювало (варте фіксації, не змін) · Патерни помилок (≥2 випадки на патерн; one-off — не патерн) · Health flags (пряма перевірка) · Пропозиції (0–3, максимум за протоколом self-improvement; рішення за Andrii) · **Не встановлено** (див. вступ). Checker цієї петлі — Andrii (паспорт L6), тож LLM-рубрики немає; рівень 0 діє як для всіх.

## gate-decision (запис у decisions-log.md)
Обов'язково: Дата · Продукт · Gate (G1–G4) · Рішення GO/KILL/PIVOT · Аргументи за/проти/ризики · Підтвердження «артефакт прочитано» · Хто (Andrii).
